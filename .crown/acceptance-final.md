# Final acceptance audit — Matterhorn v1.0.0 gate

Reviewer: independent final-acceptance pass, fresh context. Date: 2026-08-02.
Method: every check run by the reviewer; nothing accepted on the Report's word alone.

## 1. Report.md claim verification

- `pnpm validate:content` rerun by reviewer: **OK, all ten checks pass** (schema 20 artifacts, orphans 215 refs, counts, narration, lexicon 4406 strings with the noted 6.9 exemptions, manifest with both gate tokens, seed, url-index 26 entries / exactly 1 fresh_demo, liveness 44 sources, feed, size 494.4 KB / 2048 KB). Matches the Report's claim.
- Cited files exist: `content-review.md`, `LICENSES.md`, `docs/replay-protocol.md` — all present.
- `tests/e2e/__screenshots__/parity/` — present, 10 parity screenshots (8 grammar components + money-flow-stopped variant + card-compact).
- `pipeline/runs/run-2026-07-29/blocked/` — present with **exactly 7 block records**, matching the Report's "7 committed block records": judol-turnover-A10-1/2, mbg-poisoning-A11-1, mbg-stop-A11-1, tariffs-pay-A11-1, usaid-deficit-A10-1, usaid-deficit-A11-1.
- Test-count claims (162 unit etc.): unit suite rerun in section 3 below.

## 2. Zero secrets (C10 / AC-SEC)

- `find . -name ".env*"` (excluding node_modules): **one hit** — `.vercel/.env.production.local`.
  - Auto-generated by the Vercel CLI ("Created by Vercel CLI" header), dropped during a deploy/pull.
  - **Untracked and gitignored** (`.gitignore:23` covers `.vercel`); `git log --all --diff-filter=A` over `*.env*` and `.vercel` is empty — **never committed**, so git history is clean.
  - Contains `VERCEL_OIDC_TOKEN`, a development-scoped Vercel OIDC JWT, iat 2026-08-02 04:11 WIB, **exp 2026-08-02 16:11 WIB — still valid at audit time (08:48 WIB)**. Everything else in the file is non-secret CI metadata.
  - Contradicts Report.md's claim "There is no .env in this repository... deleted on the spot": the CLI has since dropped another one. The check as stated ("no .env* anywhere in the repo tree") FAILS on the working tree; the committed tree passes.
  - Remediation: delete the file (and consider `vercel env` hygiene in RUNBOOK). One command.
- `git grep -iE "(api[_-]?key|sk-ant|bearer )"` over tracked files: 2 hits, both benign —
  - `.gitleaks.toml`: a comment explaining the gitleaks generic-api-key rule exemption.
  - `pipeline/config.ts:31`: `apiKeyEnv: string;` — a field holding an env var *name*, not a credential.
- No actual keys or tokens in any tracked file.

**Section verdict: committed repo clean; working tree carries one live (until 16:11 today) Vercel OIDC token in an ignored file. Flagged as a must-delete before tag.**

## 3. Verdict-free + lexicon (C1 / AC-INV)

- `pnpm test:unit` rerun: **12 files, 162 tests, all passed** — matches Report.md's claim exactly (162 passed, 12 files).
- Verdict-word grep over all 10 narratives (EN: hoax/debunked/false/misleading/fake/untrue/lie; ID: hoaks/bohong/palsu/salah/sesat/menyesatkan/dusta): the only hits are JSON booleans (`"fallback": false`, `"severed_if_stopped": false`). Zero verdict words in prose.
- Spot-read mbg-stop.json (ID pack): narration in both languages describes structure ("two rest on no entry in the source registry... stated by no article in this family"), scale, incidence, and named alternatives. No verdict, no quality score. Register matches the calm/precise voice.
- Spot-read tariffs-pay.json (EN pack): same pattern ("step two has no entry behind it... comes from the order of two sentences rather than from what either one says"). Verdict-free holds.
- Provenance across all 10 narratives: `analyzed_by: claude-fable-5`, `narrated_by: claude-opus-5[1m]` — real executing models, no invented names.
- Minor observation (informational): mbg-stop `provenance.computed_at` is 2026-07-28 while `run_id` is run-2026-07-29; harmless naming skew, not a gate.

**Section verdict: PASS.**

## 4. No em dashes / emoji in user-facing strings (C8)

- `grep -rP "—" content/ app/src/i18n app/src/renderers/copy.ts`: **zero hits** (paths verified to exist, so the grep was not vacuous).
- Broadened sweep over all of `app/src` and `public/`: zero em dashes.
- Emoji-range grep over the same user-facing paths: zero hits.
- `pipeline/runs/` not checked by design (exempt; records stay verbatim, and Report.md discloses the one judge em dash normalized at the replay boundary).

**Section verdict: PASS.**

## 5. Live smoke (AC-DEP / AC-SEC, run by reviewer 2026-08-02 ~08:50 WIB)

- `curl -sI https://matterhorn-app.vercel.app/` — **HTTP/2 200**; CSP served: `default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'`. No `unsafe-eval`. Also HSTS preload, nosniff, DENY framing, restrictive permissions-policy.
- `/n/mbg-stop` og:image meta: `https://matterhorn-app.vercel.app/assets/og/mbg-stop.jpg` — absolute, same origin. That URL returns **200 image/jpeg** with `cache-control: public, max-age=86400, stale-while-revalidate=604800` (the revalidating stable-name imagery lane the Report claims).
- `/manifest.webmanifest`: `share_target.action` is relative `"/share"`, method GET, params title/text/url; standalone display, id `/app`, maskable icons present.

**Section verdict: PASS.**

## 6. Honesty of simulation (C6)

- Replay console: `app/src/research/replay.tsx:185` pins `data-testid="replay-disclosure"` for the entire replay; the string comes from `content/replay.json.disclosure` and reads (EN): "Replay of editorial run run-2026-07-29. Every step, model, verdict and block below is the recorded run; on-screen pacing is compressed and not to scale." ID variant equivalent. Names the run id, admits compression. PASS.
- Share flow (`app/src/routes.tsx` Share + `app/src/app/resolve.ts`): unknown URL resolves to `{ screen: 'queue' }` — the honest queue state; only the single `fresh_demo` entry gets staged progress; known URLs open the cached autopsy. Same resolver serves `/share` and the paste box (exact, then prefix, then regex). No fabricated analysis path exists. PASS.
- Provenance: all 10 narratives carry `analyzed_by: claude-fable-5`, `narrated_by: claude-opus-5[1m]`; `content/replay.json.models` lists exactly those two. Real executing models, no invented names. PASS.

**Section verdict: PASS.**

## 7. Gate ledger integrity (pipeline/runs/run-2026-07-29/blocked/)

Read all 7 records in full. Findings:

- **candidate_sha256: 6 of 7 carry a real digest; `mbg-stop-A11-1.json` carries `candidate_sha256: null`** with its own note: "Round 1. Hash pinning began the following round, so the judged bytes were not recorded as a digest at the time." The file is honest; **Report.md's claim "Every block names the candidate bytes it judged (candidate_sha256)" is false as stated.**
- The two usaid-deficit records (`usaid-deficit-A10-1.json`, `usaid-deficit-A11-1.json`) both say `reconstructed: true` with reconstruction notes and `workflow_run_id`s, and both pin the same pre-renarration digest b9027c00... — the backfill the Report discloses. Confirmed. (File suffix "-1" is per-gate block index; the A10 record's own text references "the round-1 pull-back", placing these in round 2.)
- **However, ALL SEVEN block records carry `reconstructed: true`**, each with the same class of note: "Transcribed from the run record... the runner never ingested the block and never wrote this file at the time." The entire gate ledger is an after-the-fact transcription, not a contemporaneous ledger. Report.md's Deviations section singles out only the usaid-deficit pair as reconstructed, which **understates the reconstruction scope** — the reader of Report.md would believe 5 of 7 records were written at block time. Each file is individually honest (verbatim verdict text, workflow run id); the Report's framing is not.
- Slot-record cross-check: 70 model-executed slot outputs on disk (A5-A11 x 10 narratives, inputs and outputs both recorded), model attribution exactly matches the Report (claude-opus-5[1m] 43, claude-fable-5 27). Plus 4 stage records (A1-A4) and the 7 block records. **The Report's "90 slot executions recorded" does not reconcile with any count I can produce from disk (70 / 74 / 81 by generous countings).** Minor, but it is a numeric claim in an honesty report.
- run.json generated_at 2026-07-28T21:52Z = 2026-07-29 WIB; run id naming is consistent in local time. Not an issue.

**Section verdict: ledger files individually honest and sha-pinned except one disclosed null; Report.md misdescribes the ledger in three particulars (every-block-has-sha, only-two-reconstructed, 90 executions).**

## 8. Verdict

### Exit gates (blueprint 2.3)

| Gate | Verdict | Evidence (verified by this reviewer) |
|---|---|---|
| 1. Every surface and state exists and functions | PASS | Live: `/` 200, `/n/mbg-stop` og shell, `/manifest.webmanifest`, og asset 200; share/queue/fresh-demo logic read in `app/src/routes.tsx` + `app/src/app/resolve.ts`; e2e suites and `tests/state-matrix.json` present (full Playwright matrix not rerun here; Report claims 114+51+30 green). |
| 2. Eight grammar components from typed JSON | PASS | 10 parity screenshots covering the 8 components; `validate:content` schema check green over 20 artifacts; `tests/unit/renderers/card-contract.spec.tsx` exists and unit suite is green. |
| 3. 13-agent pipeline end to end, manifests, no seed content | PASS with flags | `validate:content` manifest (both gate tokens verify) and seed checks green; full slot inputs/outputs on disk, model counts match Report exactly. Flags: whole gate ledger is reconstructed (see section 7), one block record's sha is null (disclosed in-file). |
| 4. PRD invariants as build-failing tests | PASS | `pnpm test:unit` 162/162 green incl. bilingual lexicon lint; `pnpm validate:content` ten checks green (orphans 215 refs, counts, schema, lexicon 4406 strings). |
| 5. Live on Vercel, PWA share target | PASS (human item open) | Live smoke green: 200, CSP `default-src 'self'` no unsafe-eval, absolute same-origin og:image (200), relative `/share` share_target. Physical Android share-sheet test remains human-pending per blueprint 10.3 (build does not block on it). |

### Blocking findings

1. **C10 zero-secrets, working tree: `.vercel/.env.production.local` exists and holds a Vercel OIDC token valid until 16:11 WIB today.** Untracked, gitignored, never committed — but C10 says the scan must find nothing, and the scan found this. Delete the file before tagging. One command.
2. **Report.md contains four false or unreconcilable factual claims** (an AC-DOC honesty failure in a project whose premise is honesty): "There is no .env in this repository" (there is, again); "Every block names the candidate bytes it judged" (mbg-stop-A11-1 is null); Deviations imply only the usaid-deficit pair was reconstructed (all 7 records are); "90 slot executions recorded" (disk supports 70 model executions / 81 records). Correct the four sentences before tagging.

### Non-blocking observations

- Ledger reconstruction is disclosed inside every file with workflow run ids; the product surfaces (provenance chips, replay disclosure) are honest as shipped.
- Editorial approval and the Android device checklist remain open and are honestly disclosed in the Report; blueprint 10.3 does not block the tag on them.

### Overall: **DO-NOT-TAG** until the two blocking items are cleared — delete `.vercel/.env.production.local`, and correct the four Report.md claims (env, every-block-sha, reconstruction scope, 90-executions). Both are documentation/hygiene fixes; no product or test defect was found. After those fixes this reviewer would pass v1.0.0.

## 9. Re-verification of blocking items (2026-08-02 ~09:05 WIB, after commit 8e42193)

1. **Secret file: CLEAN.** `find . -name ".env*"` excluding node_modules: zero hits, `.vercel/` included. The 09:00 deploy left `project.json`, `README.txt`, `node/`, `output/` but no fresh token file; grep over `.vercel/output/` for `VERCEL_OIDC_TOKEN` / the JWT prefix: zero hits. No new finding.
2. **Report.md corrections: all four verified against disk.**
   - (a) Env sentence now reads "No .env was ever committed... the Vercel CLI drops an untracked OIDC token file under .vercel/ on every deploy, and each one was deleted" — accurate (history clean per earlier `git log --diff-filter=A`, tree clean now).
   - (b) Ledger claim now "Six of the seven pin the candidate bytes... the earliest (mbg-stop-A11-1) predates hash pinning and says so in its own note" — matches the 7 files on disk exactly.
   - (c) Deviations now leads with "The entire block ledger is an after-the-fact transcription. All seven block records carry reconstructed: true" — matches disk.
   - (d) Generation log now "70 slot executions recorded as input and output pairs... plus 4 stage summaries and the gate ledger" — matches disk (70 model outputs with inputs, 4 stage files, 7 block records).
3. **CI fixes weakened nothing.**
   - `git show HEAD -- app/src/sw.ts`: og imagery added to the existing stale-while-revalidate lane; the empty 200 SVG answers only when BOTH cache and network miss (offline), so online 404s still surface and the card's recorded-fallback placeholder shows. Content JSON keeps its 504 `{}` miss. No assertion touched.
   - `pnpm token-audit`: green — "25 token(s), light and dark blocks in step... OK: no light-theme literal paints outside a theme scope, and every token has a dark value (AC-APP-15)."
   - Last commit touches only `.crown/acceptance-final.md`, `.github/workflows/ci.yml`, `research.css`, `surface.css`, `sw.ts`, `Report.md`. **`lighthouserc.json` and `lighthouserc.desktop.json` are untouched** — the `--no-sandbox` flag lives in ci.yml only; assertions unchanged.

### Final verdict: **TAG.** Both blockers cleared, no new findings, no check weakened.
